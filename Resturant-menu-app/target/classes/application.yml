server:
  port: 8092
spring:
  datasource:
    username: postgres
    url: jdbc:postgresql://localhost:5432/restaurant_app
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
my.name: hello